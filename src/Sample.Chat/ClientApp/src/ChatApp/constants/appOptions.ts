export interface AppOptions {
    title: string;
    subtitle?: string;
    github?: string;
}

export const appOptions: AppOptions = {
    title: 'Sample Chat app',
    subtitle: 'Sample for Azure Communication Services Chat',
    github: 'https://github.com/bbonkr/sample.azure.communication.services.chat',
};
